# 机密参考文件目录

请将您电脑上的 `机密` 文件夹内的所有文件复制到此目录。

支持格式：`.md`、`.txt`、`.sh`、`.py`、`.json`、`.yaml` 等。

上传方式（任选其一）：

1. **Cursor 对话**：把文件夹拖入聊天，或输入 `@` 选择文件
2. **SCP 上传服务器后同步**：
   ```bash
   scp -r /你的路径/机密 root@135.181.228.18:/root/机密
   ```
   上传后告诉 Agent，会从服务器拉取到此处
3. **直接复制**：把文件粘贴到本目录并提交 git
