# 破甲行动目标配置（可选）
# 填入后一键执行会自动使用
TARGET_NAME=
TARGET_BUNDLE=

# ── WiFi 网络连接（云环境无 USB，用此接上 iPhone）──
# 手机：设置 → WiFi → 点击网络 → 查看 IP
DEVICE_IP=
SSH_PORT=22
SSH_USER=root
SSH_PASS=alpine
FRIDA_PORT=27042

# 监视间隔（秒），0 = 只跑一次；接上设备后建议设 30
WATCH_INTERVAL=0
