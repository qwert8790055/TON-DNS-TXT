TONAPI_KEY=
TONCENTER_KEY=
PORT=4727

# Comma-separated CORS origins (defaults include localhost + production)
ALLOWED_ORIGINS=http://135.181.228.18:8080,https://dns.resistance.dog,http://localhost:5173

# Rate limiting: max requests per IP per window
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=30

# DNS records cache TTL in milliseconds (default 5 minutes)
DNS_CACHE_TTL_MS=300000
